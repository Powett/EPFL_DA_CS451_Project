- move stuff from Host to new class Peer
- ask about atomic/unordered_map etc use ?

- undebug 
- test w stress ?
- submit ?




- mode debug "rapide":
  - décommenter
    // DEBUG(([^/]*\n)+) *// ENDDEBUG
  - commenter
    (cout([^;]*\n*)*;)
    (ttyLog([^;]*\n*)*;)